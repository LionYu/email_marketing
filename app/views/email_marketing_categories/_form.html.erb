<%= form_with(model: email_marketing_category) do |form| %>
  <% if email_marketing_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(email_marketing_category.errors.count, "error") %> prohibited this email_marketing_category from being saved:</h2>

      <ul>
        <% email_marketing_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
